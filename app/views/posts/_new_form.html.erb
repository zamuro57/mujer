
<%= form_for(@post) do |f| %>

	<div class="field">
		<%= f.label 'Recipe Title' %>
		<%= f.text_field :title,size: 40 %>
	</div>
	<div class="actions">
		<%= f.submit remote: true %>
	</div>
<% end %>

	