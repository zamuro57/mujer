	<h2>Add a Step:</h2>
	<%= form_for([@post, @post.steps.build] , :remote => true) do |f| %>
	  <div class="field">
	    <%= f.label 'Title' %><br />
	    <%= f.text_field :title %>
	  </div>
	  <div class="field">
	    <%= f.label 'Description' %><br />
	    <%= f.text_area :content %>
	  </div>
	  <div class="actions">
	    <%= f.submit %>
	  </div>
	<% end %>